enum ContextType {
  ALBUM
  PLAYLIST
  ARTIST
  SONG
}

union RecentPlayedItem = AlbumPreview | SongPreview | ArtistPreview | PlaylistPreview


type PageInfo {
  endCursor: String
  hasNextPage: Boolean!
}