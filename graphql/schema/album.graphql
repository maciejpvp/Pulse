type Album {
  id: ID!
  name: String!
  artist: ArtistPreview!
  songs(first: Int = 10, after: String): SongConnection!
}

type AlbumPreview {
  id: ID!
  name: String!
}

input AlbumQueryInput {
  albumId: String!
  artistId: String!
}